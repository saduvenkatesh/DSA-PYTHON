# Insertion Sort
# Insertion sort method sorts a list of elements by inserting each successive element in the previously sorted sublist
# Such insertion of elements requires the other elements to be shuffled as required

class sorting :
    def __init__(self, arr):
        self.list=arr
        self.size=len(arr)
    
    def insertion_sort(self):
        pass